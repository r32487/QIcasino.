using UnityEngine;
using UnityEngine.UI;
using TMPro;
using System.Collections;
using System.Collections.Generic;

public class DragonGateMachine : MonoBehaviour
{
    // 這裡放龍門模式專用的邏輯，例如：
    // 1. 每次轉動不扣錢 (因為是贏來的次數)
    // 2. 或是扣很少錢，但中獎倍率極高
    // ... 大部分代碼可以參考 SlotMachine，但把邏輯改成「龍門專用」 ...
}